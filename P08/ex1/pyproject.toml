[tool.poetry]
name = "matrix-analysis"
version = "0.1.0"
description = "Matrix data simulation and analysis"
authors = ["Your Name <dlima-li@student.42porto.com>"]

[tool.poetry.dependencies]
python = "^3.11"
numpy = "^1.26.0"
pandas = "^2.2.0"
matplotlib = "^3.8.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
